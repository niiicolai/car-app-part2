spring.datasource.url=${JDBC_DATABASE_URL2}
spring.datasource.username=${JDBC_USERNAME}
spring.datasource.password=${JDBC_PASSWORD}

#JDBC_DATABASE_URL=jdbc:mysql://localhost:3306/db-name;JDBC_USERNAME=;JDBC_PASSWORD=

# Use this strategy for your first Hibernate exercises
spring.jpa.hibernate.ddl-auto=create-drop

# Always set this while you develop your application
spring.jpa.show-sql=true

app.token-expiration=1800
app.token-issuer=${APP_TOKEN_ISSUER}
app.secret-key=${APP_SECRET_KEY}

motor.api.url=${MOTOR_API_URL}
motor.api.key=${MOTOR_API_KEY}